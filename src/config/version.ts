/**
 * Centralized version configuration
 * Update this single location when releasing new versions
 */

export const APP_VERSION = '0.0.15.1';
export const APP_NAME = 'TweetCraft';
export const FULL_NAME = `${APP_NAME} v${APP_VERSION}`;
